/* =========================================================
   TOOLMASTER PRO
   Complete styles.css
   Compatible with the supplied main.jsx
========================================================= */

:root {
  --primary: #6d5dfc;
  --primary-dark: #5748e8;
  --primary-light: #eeeaff;

  --bg: #f7f8fc;
  --surface: #ffffff;
  --surface-2: #f1f3f9;

  --text: #171927;
  --muted: #70758a;
  --border: #e5e7ef;

  --success: #159957;
  --success-bg: #e9f9f0;

  --danger: #e5484d;
  --danger-bg: #fff0f1;

  --warning: #d68b00;
  --warning-bg: #fff8df;

  --shadow-sm: 0 4px 15px rgba(23, 25, 39, 0.05);
  --shadow: 0 12px 35px rgba(23, 25, 39, 0.08);
  --shadow-lg: 0 22px 60px rgba(23, 25, 39, 0.12);

  --radius: 16px;
  --radius-sm: 10px;
  --radius-lg: 22px;

  font-family:
    Inter,
    ui-sans-serif,
    system-ui,
    -apple-system,
    BlinkMacSystemFont,
    "Segoe UI",
    sans-serif;
}

* {
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  margin: 0;
  background: var(--bg);
  color: var(--text);
  font-family: inherit;
}

button,
input,
textarea,
select {
  font: inherit;
}

button {
  cursor: pointer;
}

button:disabled {
  cursor: not-allowed;
  opacity: 0.65;
}

a {
  color: inherit;
  text-decoration: none;
}

::selection {
  background: var(--primary);
  color: white;
}

/* =========================================================
   GLOBAL
========================================================= */

.app {
  min-height: 100vh;
  background:
    radial-gradient(
      circle at 50% 0%,
      rgba(109, 93, 252, 0.07),
      transparent 32%
    ),
    var(--bg);
}

.primary,
.secondary,
.adminBtn,
.profileBtn,
.back,
.tableAction,
.iconButton,
.linkButton,
.logoutAdmin {
  border: 0;
  outline: none;
  transition:
    transform 0.18s ease,
    background 0.18s ease,
    border-color 0.18s ease,
    color 0.18s ease,
    box-shadow 0.18s ease;
}

.primary {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  min-height: 42px;
  padding: 0 18px;
  border-radius: 10px;
  color: white;
  background: linear-gradient(
    135deg,
    var(--primary),
    #806fff
  );
  box-shadow: 0 8px 20px rgba(109, 93, 252, 0.22);
}

.primary:hover {
  transform: translateY(-1px);
  background: linear-gradient(
    135deg,
    var(--primary-dark),
    #705fff
  );
  box-shadow: 0 11px 25px rgba(109, 93, 252, 0.3);
}

.secondary {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  min-height: 42px;
  padding: 0 16px;
  border: 1px solid var(--border);
  border-radius: 10px;
  background: white;
  color: var(--text);
}

.secondary:hover {
  background: var(--surface-2);
  border-color: #d6d9e4;
}

input,
textarea,
select {
  width: 100%;
  border: 1px solid var(--border);
  border-radius: 11px;
  outline: none;
  background: white;
  color: var(--text);
  transition:
    border-color 0.18s ease,
    box-shadow 0.18s ease;
}

input:focus,
textarea:focus,
select:focus {
  border-color: var(--primary);
  box-shadow: 0 0 0 4px rgba(109, 93, 252, 0.1);
}

textarea {
  resize: vertical;
}

label {
  display: block;
  color: var(--text);
  font-size: 14px;
  font-weight: 700;
}

/* =========================================================
   HEADER
========================================================= */

header {
  position: sticky;
  top: 0;
  z-index: 100;
  border-bottom: 1px solid rgba(229, 231, 239, 0.8);
  background: rgba(255, 255, 255, 0.9);
  backdrop-filter: blur(18px);
}

.nav {
  width: min(1180px, calc(100% - 32px));
  min-height: 72px;
  margin: auto;

  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 25px;
}

.brand {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  font-size: 20px;
  font-weight: 850;
  letter-spacing: -0.5px;
  white-space: nowrap;
}

.brand > span > span {
  color: var(--primary);
}

.brandIcon {
  width: 42px;
  height: 42px;

  display: inline-flex;
  align-items: center;
  justify-content: center;

  border-radius: 12px;
  color: white;
  background: linear-gradient(
    135deg,
    var(--primary),
    #8374ff
  );

  box-shadow: 0 7px 20px rgba(109, 93, 252, 0.25);
}

.nav nav {
  display: flex;
  align-items: center;
  gap: 30px;
  margin-left: auto;
}

.nav nav a {
  color: var(--muted);
  font-size: 14px;
  font-weight: 650;
}

.nav nav a:hover {
  color: var(--primary);
}

.authActions {
  display: flex;
  align-items: center;
  gap: 9px;
}

.profileBtn,
.adminBtn {
  display: inline-flex;
  align-items: center;
  gap: 7px;

  min-height: 39px;
  padding: 0 12px;

  border-radius: 9px;
  background: var(--surface-2);
  color: var(--text);
}

.adminBtn {
  color: var(--primary);
  background: var(--primary-light);
}

.profileBtn:hover,
.adminBtn:hover {
  transform: translateY(-1px);
}

/* =========================================================
   HERO
========================================================= */

.hero {
  width: min(950px, calc(100% - 32px));
  margin: 0 auto;
  padding: 85px 0 65px;
  text-align: center;
}

.pill {
  width: fit-content;
  margin: 0 auto 22px;

  display: flex;
  align-items: center;
  gap: 7px;

  padding: 8px 13px;
  border: 1px solid #ddd8ff;
  border-radius: 999px;

  color: var(--primary);
  background: var(--primary-light);

  font-size: 13px;
  font-weight: 750;
}

.hero h1 {
  max-width: 850px;
  margin: 0 auto;

  font-size: clamp(42px, 6vw, 72px);
  line-height: 1.03;
  letter-spacing: -3.5px;
  font-weight: 900;
}

.hero h1 span {
  color: var(--primary);
}

.hero p {
  max-width: 700px;
  margin: 24px auto 30px;

  color: var(--muted);
  font-size: 17px;
  line-height: 1.7;
}

.search {
  width: min(650px, 100%);
  height: 58px;
  margin: 0 auto;

  display: flex;
  align-items: center;
  gap: 12px;

  padding: 0 18px;

  border: 1px solid var(--border);
  border-radius: 15px;

  background: white;
  box-shadow: var(--shadow);
}

.search svg {
  flex-shrink: 0;
  color: var(--muted);
}

.search input {
  height: 100%;
  padding: 0;
  border: 0;
  box-shadow: none;
  font-size: 15px;
}

.search input:focus {
  box-shadow: none;
}

.stats {
  margin: 42px auto 0;

  display: flex;
  align-items: center;
  justify-content: center;
  gap: 70px;
}

.stats div {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.stats b {
  font-size: 25px;
  letter-spacing: -0.5px;
}

.stats small {
  color: var(--muted);
  font-size: 12px;
  font-weight: 650;
}

/* =========================================================
   CATEGORIES
========================================================= */

main#tools {
  width: min(1180px, calc(100% - 32px));
  margin: 0 auto;
}

.categories {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 10px;
  margin-bottom: 55px;
}

.cat {
  min-height: 54px;

  display: flex;
  align-items: center;
  gap: 9px;

  padding: 0 13px;

  border: 1px solid var(--border);
  border-radius: 12px;

  background: white;
  color: var(--muted);

  font-size: 12px;
  font-weight: 700;

  transition: all 0.18s ease;
}

.cat svg {
  width: 17px;
  height: 17px;
  flex-shrink: 0;
}

.cat em {
  margin-left: auto;

  min-width: 25px;
  padding: 3px 6px;

  border-radius: 999px;
  background: var(--surface-2);

  font-size: 10px;
  font-style: normal;
  text-align: center;
}

.cat:hover {
  border-color: #cfc9ff;
  color: var(--primary);
  transform: translateY(-1px);
}

.cat.active {
  border-color: var(--primary);
  color: var(--primary);
  background: var(--primary-light);
}

.cat.active em {
  background: white;
  color: var(--primary);
}

.sectionHead {
  display: flex;
  align-items: end;
  justify-content: space-between;
  margin-bottom: 22px;
}

.sectionHead h2 {
  margin: 0;
  font-size: 28px;
  letter-spacing: -1px;
}

.sectionHead p {
  margin: 6px 0 0;
  color: var(--muted);
  font-size: 13px;
}

/* =========================================================
   TOOL CARDS
========================================================= */

.grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 15px;
  padding-bottom: 85px;
}

.card {
  position: relative;

  min-height: 190px;
  padding: 20px;

  border: 1px solid var(--border);
  border-radius: var(--radius);

  background: white;
  box-shadow: var(--shadow-sm);

  cursor: pointer;
  overflow: hidden;

  transition:
    transform 0.2s ease,
    box-shadow 0.2s ease,
    border-color 0.2s ease;
}

.card:hover {
  transform: translateY(-5px);
  border-color: #d6d0ff;
  box-shadow: var(--shadow);
}

.toolIcon {
  width: 43px;
  height: 43px;

  display: inline-flex;
  align-items: center;
  justify-content: center;

  border-radius: 12px;
  color: var(--primary);
  background: var(--primary-light);
}

.toolIcon.big {
  width: 65px;
  height: 65px;
  border-radius: 17px;
}

.cardBody {
  margin-top: 17px;
  padding-right: 18px;
}

.cardBody > span {
  color: var(--primary);
  font-size: 10px;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.7px;
}

.card h3 {
  margin: 7px 0 7px;
  font-size: 16px;
  letter-spacing: -0.25px;
}

.card p {
  margin: 0;

  color: var(--muted);
  font-size: 12px;
  line-height: 1.55;
}

.arrow {
  position: absolute;
  right: 17px;
  bottom: 17px;
  color: #b3b6c2;
  transition: transform 0.2s ease, color 0.2s ease;
}

.card:hover .arrow {
  color: var(--primary);
  transform: translateX(3px);
}

.empty {
  padding: 70px 20px;
  margin-bottom: 70px;

  border: 1px dashed var(--border);
  border-radius: var(--radius);

  color: var(--muted);
  text-align: center;
}

/* =========================================================
   FOOTER
========================================================= */

footer {
  padding: 55px 20px;

  border-top: 1px solid var(--border);
  background: white;

  text-align: center;
}

footer .brand {
  justify-content: center;
}

footer p {
  margin: 15px 0 8px;
  color: var(--muted);
}

footer small {
  color: #9a9eac;
  font-size: 12px;
}

/* =========================================================
   AUTH
========================================================= */

.authPage {
  min-height: calc(100vh - 73px);

  display: flex;
  align-items: center;
  justify-content: center;

  position: relative;
  padding: 60px 20px;

  background:
    radial-gradient(
      circle at 50% 0%,
      rgba(109, 93, 252, 0.1),
      transparent 35%
    ),
    var(--bg);
}

.authBack {
  position: absolute;
  top: 25px;
  left: 25px;
}

.back {
  display: inline-flex;
  align-items: center;
  gap: 7px;

  padding: 9px 12px;

  border-radius: 9px;
  background: transparent;

  color: var(--muted);
  font-size: 13px;
  font-weight: 700;
}

.back:hover {
  color: var(--primary);
  background: var(--primary-light);
}

.authCard {
  position: relative;

  width: min(460px, 100%);
  padding: 38px;

  border: 1px solid var(--border);
  border-radius: 22px;

  background: white;
  box-shadow: var(--shadow-lg);
}

.closeAuth {
  position: absolute;
  top: 16px;
  right: 16px;

  width: 34px;
  height: 34px;

  display: flex;
  align-items: center;
  justify-content: center;

  border: 0;
  border-radius: 9px;

  background: var(--surface-2);
  color: var(--muted);
}

.closeAuth:hover {
  color: var(--danger);
}

.authLogo {
  margin-bottom: 20px;
}

.authCard h1 {
  margin: 0 0 8px;

  font-size: 28px;
  letter-spacing: -1px;
}

.authCard > p {
  margin: 0 0 28px;
  color: var(--muted);
  line-height: 1.55;
  font-size: 14px;
}

.authForm {
  display: flex;
  flex-direction: column;
  gap: 17px;
}

.authForm label {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.inputIcon {
  position: relative;
  display: flex;
  align-items: center;
}

.inputIcon > svg {
  position: absolute;
  left: 13px;
  color: #9a9eac;
  pointer-events: none;
}

.inputIcon input {
  height: 46px;
  padding: 0 42px;
}

.iconButton {
  position: absolute;
  right: 7px;

  width: 32px;
  height: 32px;

  display: flex;
  align-items: center;
  justify-content: center;

  border-radius: 8px;
  background: transparent;
  color: var(--muted);
}

.iconButton:hover {
  background: var(--surface-2);
}

.authRow {
  display: flex;
  justify-content: flex-end;
}

.linkButton {
  padding: 0;
  background: transparent;
  color: var(--primary);
  font-size: 12px;
  font-weight: 700;
}

.linkButton:hover {
  text-decoration: underline;
}

.authSubmit {
  width: 100%;
  min-height: 48px;
  margin-top: 2px;
}

.authBottom {
  margin: 2px 0 0 !important;
  text-align: center;
  font-size: 13px !important;
}

.authBottom button {
  margin-left: 5px;
  border: 0;
  background: transparent;
  color: var(--primary);
  font-weight: 800;
}

.errorBox,
.successBox,
.notice,
.statusBox {
  display: flex;
  align-items: center;
  gap: 8px;

  padding: 12px 14px;
  border-radius: 10px;

  font-size: 13px;
  line-height: 1.5;
}

.errorBox {
  border: 1px solid #ffd2d5;
  background: var(--danger-bg);
  color: #b4232a;
}

.successBox {
  border: 1px solid #c7efd8;
  background: var(--success-bg);
  color: #087443;
}

.notice {
  border: 1px solid #ddd8ff;
  background: var(--primary-light);
  color: #5044ba;
}

.statusBox {
  margin-top: 16px;
  border: 1px solid #ddd8ff;
  background: var(--primary-light);
  color: #5044ba;
}

/* =========================================================
   TOOL PAGE
========================================================= */

.toolPage {
  width: min(1080px, calc(100% - 32px));
  min-height: calc(100vh - 73px);

  margin: auto;
  padding: 42px 0 80px;
}

.toolHero {
  display: flex;
  align-items: center;
  gap: 20px;

  margin: 28px 0 35px;
}

.toolHero > div:last-child > span {
  color: var(--primary);
  font-size: 11px;
  font-weight: 850;
  text-transform: uppercase;
  letter-spacing: 1px;
}

.toolHero h1 {
  margin: 7px 0 8px;

  font-size: clamp(30px, 5vw, 44px);
  line-height: 1.05;
  letter-spacing: -1.7px;
}

.toolHero p {
  margin: 0;
  color: var(--muted);
  line-height: 1.6;
}

.workspace {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 18px;
}

.panel {
  padding: 22px;

  border: 1px solid var(--border);
  border-radius: var(--radius);

  background: white;
  box-shadow: var(--shadow-sm);
}

.panel label {
  display: block;
  margin-bottom: 10px;
}

.panel textarea {
  min-height: 310px;
  padding: 15px;

  font-size: 14px;
  line-height: 1.6;
}

.actions {
  display: flex;
  flex-wrap: wrap;
  gap: 9px;
  margin-top: 13px;
}

.actions button {
  min-height: 40px;
}

.fileWorkspace {
  padding: 25px;

  border: 1px solid var(--border);
  border-radius: var(--radius);

  background: white;
  box-shadow: var(--shadow-sm);
}

.uploadBox {
  min-height: 160px;

  display: flex;
  align-items: center;
  justify-content: center;
  gap: 15px;

  padding: 25px;

  border: 2px dashed #d9d5ff;
  border-radius: 16px;

  background: #faf9ff;
  color: var(--primary);

  text-align: center;
  cursor: pointer;

  transition: all 0.2s ease;
}

.uploadBox:hover {
  border-color: var(--primary);
  background: var(--primary-light);
}

.uploadBox > div {
  display: flex;
  flex-direction: column;
  gap: 5px;
}

.uploadBox b {
  color: var(--text);
}

.uploadBox small {
  color: var(--muted);
}

.uploadBox strong {
  color: var(--primary);
  word-break: break-all;
}

.uploadBox input {
  display: none;
}

.fileActions {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin-top: 18px;
}

.downloadBtn {
  margin-top: 12px;
}

/* =========================================================
   AI TOOLS
========================================================= */

.aiHelper {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 18px;
}

.aiCard {
  padding: 24px;

  border: 1px solid var(--border);
  border-radius: var(--radius);

  background: white;
  box-shadow: var(--shadow-sm);
}

.aiCard h3 {
  margin: 0 0 16px;
  font-size: 17px;
}

.aiCard textarea {
  min-height: 220px;
  padding: 15px;

  line-height: 1.6;
}

.aiSolve {
  width: 100%;
  min-height: 47px;
  margin-top: 13px;
}

.resultCard {
  display: flex;
  flex-direction: column;
}

.answer {
  flex: 1;
  min-height: 220px;

  margin-bottom: 13px;
  padding: 17px;

  border: 1px solid var(--border);
  border-radius: 12px;

  background: var(--surface-2);

  white-space: pre-wrap;
  line-height: 1.7;
  font-size: 14px;
  color: var(--muted);
}

.resultCard > button {
  margin-top: 8px;
  width: 100%;
}

.videoOptions {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 12px;
  margin-top: 13px;
}

.videoOptions label {
  display: flex;
  flex-direction: column;
  gap: 7px;
}

.videoOptions select {
  height: 43px;
  padding: 0 12px;
}

/* =========================================================
   TEXT TO VIDEO
========================================================= */

.creditBar {
  display: flex;
  justify-content: flex-end;

  margin-bottom: 15px;
}

.creditBar > div {
  display: flex;
  align-items: center;
  gap: 8px;

  padding: 9px 13px;

  border: 1px solid #ddd8ff;
  border-radius: 10px;

  background: var(--primary-light);
  color: var(--primary);
}

.creditBar b {
  font-size: 18px;
}

.creditBar span {
  font-size: 12px;
  font-weight: 700;
}

.plansGrid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 12px;
  margin-bottom: 18px;
}

.planCard {
  padding: 18px;

  border: 1px solid var(--border);
  border-radius: 14px;

  background: white;
}

.planCard span,
.planCard small {
  display: block;
}

.planCard span {
  color: var(--muted);
  font-size: 12px;
  font-weight: 750;
}

.planCard b {
  display: block;
  margin: 7px 0;
  font-size: 22px;
}

.planCard small {
  color: var(--muted);
  line-height: 1.5;
}

.videoPlaceholder {
  min-height: 300px;

  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;

  gap: 10px;

  border: 1px dashed var(--border);
  border-radius: 15px;

  background:
    linear-gradient(
      135deg,
      rgba(109, 93, 252, 0.04),
      rgba(109, 93, 252, 0.01)
    );

  text-align: center;
}

.playCircle {
  width: 60px;
  height: 60px;

  display: flex;
  align-items: center;
  justify-content: center;

  border-radius: 50%;
  background: var(--primary);
  color: white;

  box-shadow: 0 10px 25px rgba(109, 93, 252, 0.25);
}

.videoPlaceholder b {
  font-size: 14px;
}

.videoPlaceholder small {
  color: var(--muted);
}

/* =========================================================
   ADMIN LAYOUT
========================================================= */

.adminShell {
  min-height: 100vh;
  display: flex;
  background: #f5f6fa;
}

.adminSidebar {
  position: fixed;
  left: 0;
  top: 0;
  bottom: 0;

  width: 250px;

  display: flex;
  flex-direction: column;

  padding: 22px 15px;

  background: #11131f;
  color: white;

  z-index: 200;
}

.adminBrand {
  display: flex;
  align-items: center;
  gap: 10px;

  padding: 4px 8px 25px;
}

.adminBrand .brandIcon {
  width: 38px;
  height: 38px;
}

.adminBrand strong,
.adminBrand span {
  display: block;
}

.adminBrand strong {
  font-size: 15px;
}

.adminBrand span {
  margin-top: 2px;
  color: #898da0;
  font-size: 10px;
}

.adminNav {
  flex: 1;
}

.adminNav > small {
  display: block;
  padding: 12px 11px;

  color: #686d82;
  font-size: 9px;
  font-weight: 800;
  letter-spacing: 1px;
}

.adminNavItem {
  width: 100%;
  min-height: 44px;

  display: flex;
  align-items: center;
  gap: 11px;

  padding: 0 11px;
  margin-bottom: 4px;

  border: 0;
  border-radius: 9px;

  background: transparent;
  color: #9ca0b2;

  text-align: left;
}

.adminNavItem span {
  flex: 1;
  font-size: 12px;
  font-weight: 650;
}

.adminNavItem > svg:last-child {
  opacity: 0;
}

.adminNavItem:hover {
  background: #1b1e2c;
  color: white;
}

.adminNavItem.active {
  background: rgba(109, 93, 252, 0.17);
  color: #a79cff;
}

.adminNavItem.active > svg:last-child {
  opacity: 1;
}

.adminSidebarBottom {
  border-top: 1px solid #252837;
  padding-top: 15px;
}

.adminMiniProfile {
  display: flex;
  align-items: center;
  gap: 10px;

  padding: 8px;
}

.adminMiniProfile strong,
.adminMiniProfile span {
  display: block;
}

.adminMiniProfile strong {
  max-width: 135px;

  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;

  font-size: 11px;
}

.adminMiniProfile span {
  max-width: 135px;

  margin-top: 3px;

  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;

  color: #73778b;
  font-size: 9px;
}

.avatar {
  width: 35px;
  height: 35px;

  flex-shrink: 0;

  display: flex;
  align-items: center;
  justify-content: center;

  border-radius: 10px;

  background: linear-gradient(
    135deg,
    var(--primary),
    #897aff
  );

  color: white;
  font-size: 12px;
  font-weight: 800;
}

.logoutAdmin {
  width: 100%;
  min-height: 39px;

  display: flex;
  align-items: center;
  gap: 9px;

  padding: 0 10px;

  border-radius: 8px;
  background: transparent;
  color: #8d91a4;

  font-size: 11px;
  font-weight: 700;
}

.logoutAdmin:hover {
  background: #201b25;
  color: #ff8589;
}

/* =========================================================
   ADMIN MAIN
========================================================= */

.adminMain {
  width: calc(100% - 250px);
  min-height: 100vh;
  margin-left: 250px;
}

.adminHeader {
  min-height: 80px;

  display: flex;
  align-items: center;
  justify-content: space-between;

  padding: 0 30px;

  border-bottom: 1px solid var(--border);

  background: white;
}

.adminEyebrow {
  color: var(--primary);
  font-size: 9px;
  font-weight: 850;
  letter-spacing: 1.2px;
}

.adminHeader h1 {
  margin: 4px 0 0;

  font-size: 23px;
  letter-spacing: -0.8px;
}

.adminHeaderActions {
  display: flex;
  align-items: center;
  gap: 10px;
}

.adminHeaderUser {
  padding-left: 8px;
  border-left: 1px solid var(--border);
}

.mobileAdminMenu {
  display: none;
}

.adminNotice {
  margin: 18px 30px 0;
  justify-content: space-between;
}

.adminNotice button {
  border: 0;
  background: transparent;
  color: inherit;
}

/* =========================================================
   ADMIN CONTENT
========================================================= */

.adminContent {
  padding: 30px;
}

.welcomeAdmin {
  min-height: 145px;

  display: flex;
  align-items: center;
  justify-content: space-between;

  padding: 28px;

  border: 1px solid #ddd8ff;
  border-radius: 18px;

  background:
    radial-gradient(
      circle at 80% 20%,
      rgba(109, 93, 252, 0.12),
      transparent 35%
    ),
    white;

  box-shadow: var(--shadow-sm);
}

.welcomeAdmin > div:first-child > span {
  color: var(--primary);
  font-size: 10px;
  font-weight: 850;
  letter-spacing: 1px;
}

.welcomeAdmin h2 {
  margin: 8px 0 7px;
  font-size: 25px;
  letter-spacing: -1px;
}

.welcomeAdmin p {
  margin: 0;
  color: var(--muted);
  font-size: 13px;
}

.systemBadge {
  display: flex;
  align-items: center;
  gap: 8px;

  padding: 9px 12px;

  border: 1px solid #c7efd8;
  border-radius: 999px;

  background: var(--success-bg);
  color: var(--success);

  font-size: 11px;
  font-weight: 750;
}

.systemBadge span {
  width: 7px;
  height: 7px;
  border-radius: 50%;
  background: var(--success);
}

.dashboardStats {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 14px;

  margin-top: 18px;
}

.dashboardStat {
  display: flex;
  align-items: center;
  gap: 13px;

  padding: 18px;

  border: 1px solid var(--border);
  border-radius: 14px;

  background: white;
}

.statIcon {
  width: 43px;
  height: 43px;

  display: flex;
  align-items: center;
  justify-content: center;

  border-radius: 11px;
  background: var(--primary-light);
  color: var(--primary);
}

.statInfo span,
.statInfo small {
  display: block;
}

.statInfo span {
  color: var(--muted);
  font-size: 11px;
}

.statInfo strong {
  display: block;
  margin: 3px 0;

  font-size: 21px;
}

.statInfo small {
  color: var(--success);
  font-size: 9px;
  font-weight: 750;
}

.adminTwoCol {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 18px;
  margin-top: 18px;
}

.adminPanelCard,
.settingsCard,
.adminTableCard,
.activityCard {
  border: 1px solid var(--border);
  border-radius: 15px;
  background: white;
  box-shadow: var(--shadow-sm);
}

.adminPanelCard {
  padding: 22px;
}

.panelTitle {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 15px;

  margin-bottom: 20px;
}

.panelTitle h3 {
  margin: 0 0 5px;
  font-size: 15px;
}

.panelTitle p {
  margin: 0;
  color: var(--muted);
  font-size: 11px;
}

.panelTitle > svg {
  color: var(--primary);
}

.overviewRows {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.overviewRows div {
  display: flex;
  align-items: center;
  justify-content: space-between;

  padding: 11px 13px;

  border-radius: 9px;
  background: var(--surface-2);
}

.overviewRows span {
  color: var(--muted);
  font-size: 11px;
}

.overviewRows b {
  font-size: 13px;
}

.quickActions {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 9px;
}

.quickActions button {
  min-height: 52px;

  display: flex;
  align-items: center;
  gap: 8px;

  padding: 0 12px;

  border: 1px solid var(--border);
  border-radius: 10px;

  background: white;
  color: var(--text);

  font-size: 11px;
  font-weight: 700;
}

.quickActions button:hover {
  border-color: #d4ceff;
  background: var(--primary-light);
  color: var(--primary);
}

/* =========================================================
   ADMIN TOOLBAR
========================================================= */

.pageToolbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 15px;

  margin-bottom: 20px;
}

.pageToolbar h2 {
  margin: 0 0 5px;

  font-size: 23px;
  letter-spacing: -0.8px;
}

.pageToolbar p {
  margin: 0;
  color: var(--muted);
  font-size: 12px;
}

.adminSearch {
  width: min(450px, 100%);
  height: 44px;

  display: flex;
  align-items: center;
  gap: 9px;

  margin-bottom: 15px;
  padding: 0 13px;

  border: 1px solid var(--border);
  border-radius: 10px;

  background: white;
}

.adminSearch svg {
  color: var(--muted);
}

.adminSearch input {
  height: 100%;
  padding: 0;
  border: 0;
  box-shadow: none;
  font-size: 12px;
}

.adminSearch input:focus {
  box-shadow: none;
}

/* =========================================================
   ADMIN USERS TABLE
========================================================= */

.adminTableCard {
  overflow: hidden;
}

.tableWrap {
  width: 100%;
  overflow-x: auto;
}

table {
  width: 100%;
  border-collapse: collapse;
  min-width: 800px;
}

th {
  padding: 14px 16px;

  border-bottom: 1px solid var(--border);

  background: #fafbfe;

  color: #85899a;

  font-size: 9px;
  font-weight: 850;
  text-align: left;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

td {
  padding: 14px 16px;
  border-bottom: 1px solid #f0f1f5;

  color: #505467;
  font-size: 11px;
}

tr:last-child td {
  border-bottom: 0;
}

.tableUser {
  display: flex;
  align-items: center;
  gap: 9px;
}

.tableUser strong,
.tableUser span {
  display: block;
}

.tableUser strong {
  color: var(--text);
  font-size: 11px;
}

.tableUser span {
  margin-top: 2px;
  color: #999dab;
  font-size: 9px;
}

.roleBadge {
  display: inline-flex;

  padding: 4px 8px;

  border-radius: 999px;

  background: var(--surface-2);
  color: var(--muted);

  font-size: 9px;
  font-weight: 800;
}

.roleBadge.admin {
  background: var(--primary-light);
  color: var(--primary);
}

.tableAction {
  width: 30px;
  height: 30px;

  display: inline-flex;
  align-items: center;
  justify-content: center;

  margin-right: 5px;

  border-radius: 8px;
  background: var(--surface-2);
  color: var(--muted);
}

.tableAction:hover {
  color: var(--primary);
  background: var(--primary-light);
}

.tableAction.danger:hover {
  color: var(--danger);
  background: var(--danger-bg);
}

.loadingAdmin,
.emptyAdmin {
  padding: 70px 20px;
  text-align: center;
}

.emptyAdmin svg {
  color: var(--primary);
}

.emptyAdmin h3 {
  margin: 12px 0 6px;
}

.emptyAdmin p {
  max-width: 450px;
  margin: auto;
  color: var(--muted);
  font-size: 12px;
  line-height: 1.6;
}

/* =========================================================
   ADMIN TOOLS
========================================================= */

.toolAdminGrid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 12px;
}

.toolAdminCard {
  min-height: 75px;

  display: flex;
  align-items: center;
  gap: 11px;

  padding: 13px;

  border: 1px solid var(--border);
  border-radius: 12px;

  background: white;
}

.toolAdminIcon {
  width: 38px;
  height: 38px;

  display: flex;
  align-items: center;
  justify-content: center;

  flex-shrink: 0;

  border-radius: 10px;
  background: var(--primary-light);
  color: var(--primary);
}

.toolAdminInfo {
  flex: 1;
  min-width: 0;
}

.toolAdminInfo strong,
.toolAdminInfo span {
  display: block;
}

.toolAdminInfo strong {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;

  font-size: 11px;
}

.toolAdminInfo span {
  margin-top: 3px;
  color: var(--muted);
  font-size: 9px;
}

.toolAdminActions {
  display: flex;
  gap: 4px;
}

.toolAdminActions button,
.categoryAdminCard > button {
  width: 29px;
  height: 29px;

  display: flex;
  align-items: center;
  justify-content: center;

  border: 0;
  border-radius: 8px;

  background: var(--surface-2);
  color: var(--muted);
}

.toolAdminActions button:hover,
.categoryAdminCard > button:hover {
  color: var(--primary);
  background: var(--primary-light);
}

/* =========================================================
   ADMIN CATEGORIES
========================================================= */

.categoryAdminGrid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 13px;
}

.categoryAdminCard {
  display: flex;
  align-items: center;
  gap: 11px;

  padding: 17px;

  border: 1px solid var(--border);
  border-radius: 13px;

  background: white;
}

.categoryAdminIcon {
  width: 42px;
  height: 42px;

  display: flex;
  align-items: center;
  justify-content: center;

  flex-shrink: 0;

  border-radius: 11px;
  background: var(--primary-light);
  color: var(--primary);
}

.categoryAdminCard > div:nth-child(2) {
  flex: 1;
}

.categoryAdminCard strong,
.categoryAdminCard span {
  display: block;
}

.categoryAdminCard strong {
  font-size: 12px;
}

.categoryAdminCard span {
  margin-top: 4px;
  color: var(--muted);
  font-size: 10px;
}

/* =========================================================
   ANALYTICS
========================================================= */

.analyticsCards {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 13px;
}

.analyticsCards > div {
  padding: 20px;

  border: 1px solid var(--border);
  border-radius: 14px;

  background: white;
}

.analyticsCards span,
.analyticsCards small {
  display: block;
}

.analyticsCards span {
  color: var(--muted);
  font-size: 10px;
  font-weight: 700;
}

.analyticsCards strong {
  display: block;
  margin: 8px 0;

  font-size: 25px;
}

.analyticsCards small {
  color: #999dab;
  font-size: 9px;
}

.chartPlaceholder {
  min-height: 300px;

  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;

  margin-top: 18px;

  border: 1px dashed var(--border);
  border-radius: 15px;

  background: white;

  text-align: center;
}

.chartPlaceholder svg {
  color: var(--primary);
}

.chartPlaceholder h3 {
  margin: 12px 0 5px;
}

.chartPlaceholder p {
  margin: 0;
  color: var(--muted);
  font-size: 12px;
}

/* =========================================================
   ADMIN PLANS
========================================================= */

.planAdminGrid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 14px;
}

.adminPlanCard {
  position: relative;

  padding: 23px;

  border: 1px solid var(--border);
  border-radius: 15px;

  background: white;
}

.adminPlanCard.popular {
  border-color: #c9c2ff;
  box-shadow: 0 10px 30px rgba(109, 93, 252, 0.08);
}

.adminPlanCard > em {
  position: absolute;
  top: 13px;
  right: 13px;

  padding: 4px 7px;

  border-radius: 999px;

  background: var(--primary-light);
  color: var(--primary);

  font-size: 8px;
  font-style: normal;
  font-weight: 850;
}

.adminPlanCard h3 {
  margin: 0;
  font-size: 16px;
}

.adminPlanCard > strong {
  display: block;
  margin: 18px 0 4px;

  font-size: 29px;
}

.adminPlanCard > span {
  display: block;
  color: var(--primary);
  font-size: 10px;
  font-weight: 750;
}

.adminPlanCard p {
  min-height: 38px;

  color: var(--muted);
  font-size: 11px;
  line-height: 1.5;
}

.adminPlanCard .secondary {
  width: 100%;
  min-height: 38px;
  font-size: 11px;
}

/* =========================================================
   SETTINGS
========================================================= */

.settingsGrid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 18px;
}

.settingsCard {
  padding: 23px;
}

.settingsCard label {
  margin-top: 18px;
}

.settingsCard input,
.settingsCard textarea {
  margin-top: 8px;
  padding: 11px 12px;
}

.settingsCard input {
  height: 44px;
}

.settingsCard textarea {
  min-height: 120px;
}

.settingToggle {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 15px;

  padding: 15px 0;
  border-bottom: 1px solid var(--border);
}

.settingToggle:last-child {
  border-bottom: 0;
}

.settingToggle strong,
.settingToggle span {
  display: block;
}

.settingToggle strong {
  font-size: 12px;
}

.settingToggle span {
  margin-top: 4px;
  color: var(--muted);
  font-size: 10px;
}

.toggle {
  position: relative;

  width: 46px;
  height: 25px;

  flex-shrink: 0;

  padding: 0;

  border: 0;
  border-radius: 999px;

  background: #d7d9e2;
}

.toggle span {
  position: absolute;
  top: 4px;
  left: 4px;

  width: 17px;
  height: 17px;

  margin: 0;

  border-radius: 50%;
  background: white;

  transition: transform 0.2s ease;
}

.toggle.on {
  background: var(--primary);
}

.toggle.on span {
  transform: translateX(21px);
}

/* =========================================================
   ACTIVITY
========================================================= */

.activityCard {
  overflow: hidden;
}

.activityItem {
  display: flex;
  align-items: center;
  gap: 13px;

  padding: 17px 20px;

  border-bottom: 1px solid var(--border);
}

.activityItem:last-child {
  border-bottom: 0;
}

.activityIcon {
  width: 37px;
  height: 37px;

  display: flex;
  align-items: center;
  justify-content: center;

  flex-shrink: 0;

  border-radius: 10px;
  background: var(--primary-light);
  color: var(--primary);
}

.activityItem > div:nth-child(2) {
  flex: 1;
}

.activityItem strong,
.activityItem span {
  display: block;
}

.activityItem strong {
  font-size: 12px;
}

.activityItem span {
  margin-top: 4px;
  color: var(--muted);
  font-size: 10px;
  line-height: 1.5;
}

.activityItem time {
  color: #999dab;
  font-size: 9px;
}

/* =========================================================
   SCROLLBAR
========================================================= */

::-webkit-scrollbar {
  width: 8px;
  height: 8px;
}

::-webkit-scrollbar-track {
  background: #f2f3f7;
}

::-webkit-scrollbar-thumb {
  border-radius: 999px;
  background: #c8cad5;
}

::-webkit-scrollbar-thumb:hover {
  background: #aaaebb;
}

/* =========================================================
   RESPONSIVE - TABLET
========================================================= */

@media (max-width: 1050px) {
  .grid {
    grid-template-columns: repeat(3, 1fr);
  }

  .categories {
    grid-template-columns: repeat(3, 1fr);
  }

  .dashboardStats {
    grid-template-columns: repeat(2, 1fr);
  }

  .toolAdminGrid,
  .categoryAdminGrid {
    grid-template-columns: repeat(2, 1fr);
  }

  .planAdminGrid {
    grid-template-columns: repeat(2, 1fr);
  }

  .analyticsCards {
    grid-template-columns: repeat(2, 1fr);
  }
}

/* =========================================================
   RESPONSIVE - MOBILE
========================================================= */

@media (max-width: 780px) {
  .nav {
    min-height: 65px;
  }

  .nav nav {
    display: none;
  }

  .authActions .profileBtn {
    display: none;
  }

  .hero {
    padding-top: 55px;
  }

  .hero h1 {
    font-size: 43px;
    letter-spacing: -2px;
  }

  .hero p {
    font-size: 14px;
  }

  .stats {
    gap: 30px;
  }

  .stats b {
    font-size: 21px;
  }

  .categories {
    grid-template-columns: repeat(2, 1fr);
  }

  .grid {
    grid-template-columns: repeat(2, 1fr);
  }

  .workspace,
  .aiHelper,
  .adminTwoCol,
  .settingsGrid {
    grid-template-columns: 1fr;
  }

  .plansGrid {
    grid-template-columns: repeat(2, 1fr);
  }

  .videoOptions {
    grid-template-columns: 1fr;
  }

  /* Admin mobile */

  .adminSidebar {
    width: 250px;
    transform: translateX(-100%);
    transition: transform 0.25s ease;
    box-shadow: 15px 0 40px rgba(0, 0, 0, 0.2);
  }

  .adminSidebar.open {
    transform: translateX(0);
  }

  .adminMain {
    width: 100%;
    margin-left: 0;
  }

  .mobileAdminMenu {
    width: 38px;
    height: 38px;

    display: flex;
    align-items: center;
    justify-content: center;

    margin-right: 10px;

    border: 0;
    border-radius: 9px;

    background: var(--surface-2);
    color: var(--text);
  }

  .adminHeader {
    padding: 0 16px;
  }

  .adminHeader {
    justify-content: flex-start;
  }

  .adminHeader > div:nth-child(2) {
    flex: 1;
  }

  .adminHeaderActions .secondary {
    display: none;
  }

  .adminContent {
    padding: 20px 15px;
  }

  .welcomeAdmin {
    flex-direction: column;
    align-items: flex-start;
    gap: 18px;
  }

  .dashboardStats {
    grid-template-columns: 1fr 1fr;
  }

  .toolAdminGrid,
  .categoryAdminGrid,
  .analyticsCards,
  .planAdminGrid {
    grid-template-columns: 1fr;
  }

  .pageToolbar {
    align-items: flex-start;
    flex-direction: column;
  }
}

/* =========================================================
   RESPONSIVE - SMALL MOBILE
========================================================= */

@media (max-width: 520px) {
  .nav {
    width: calc(100% - 20px);
  }

  .brand {
    font-size: 17px;
  }

  .brandIcon {
    width: 36px;
    height: 36px;
  }

  .authActions {
    gap: 5px;
  }

  .authActions .secondary {
    padding: 0 10px;
  }

  .authActions .primary {
    padding: 0 11px;
  }

  .hero {
    width: calc(100% - 22px);
    padding: 45px 0 45px;
  }

  .hero h1 {
    font-size: 36px;
  }

  .hero p {
    font-size: 13px;
  }

  .stats {
    gap: 20px;
  }

  .stats b {
    font-size: 18px;
  }

  .stats small {
    font-size: 10px;
  }

  main#tools {
    width: calc(100% - 22px);
  }

  .categories {
    grid-template-columns: 1fr 1fr;
    gap: 7px;
    margin-bottom: 40px;
  }

  .cat {
    min-height: 49px;
    padding: 0 9px;
    font-size: 10px;
  }

  .cat em {
    min-width: 21px;
  }

  .grid {
    grid-template-columns: 1fr;
  }

  .toolPage {
    width: calc(100% - 22px);
    padding-top: 25px;
  }

  .toolHero {
    align-items: flex-start;
  }

  .toolHero h1 {
    font-size: 29px;
  }

  .toolHero p {
    font-size: 12px;
  }

  .toolIcon.big {
    width: 52px;
    height: 52px;
  }

  .panel,
  .aiCard,
  .fileWorkspace {
    padding: 16px;
  }

  .panel textarea,
  .aiCard textarea {
    min-height: 230px;
  }

  .actions,
  .fileActions {
    flex-direction: column;
  }

  .actions button,
  .fileActions button {
    width: 100%;
  }

  .plansGrid {
    grid-template-columns: 1fr;
  }

  .creditBar {
    justify-content: stretch;
  }

  .creditBar > div {
    width: 100%;
    justify-content: center;
  }

  .authCard {
    padding: 28px 20px;
  }

  .authBack {
    left: 12px;
    top: 12px;
  }

  .authPage {
    padding: 70px 12px 30px;
  }

  .dashboardStats {
    grid-template-columns: 1fr;
  }

  .quickActions {
    grid-template-columns: 1fr;
  }

  .systemBadge {
    font-size: 9px;
  }

  .welcomeAdmin h2 {
    font-size: 21px;
  }

  .adminHeader h1 {
    font-size: 19px;
  }
}

/* =========================================================
   ACCESSIBILITY
========================================================= */

button:focus-visible,
a:focus-visible,
input:focus-visible,
textarea:focus-visible,
select:focus-visible {
  outline: 3px solid rgba(109, 93, 252, 0.25);
  outline-offset: 2px;
}

/* =========================================================
   REDUCED MOTION
========================================================= */

@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    scroll-behavior: auto !important;
    transition: none !important;
  }
}
